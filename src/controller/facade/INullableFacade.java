package controller.facade;

/**
 *
 */
public interface INullableFacade {
    /**
     * Say if an expression is rational, using a filename.
     * @param args filename in args.
     * @return
     */
    String nullable(String[] args);
}
